# 📝 إصلاح وحدة العلف (Feed Module)

## المشكلة
1. **في تطبيق الجوال (Mobile)**: شاشة استهلاك العلف لا تحتوي على زر إضافة سجل، ولا يوجد تخصيص للمزرعة
2. **في تطبيق سطح المكتب (Desktop)**: شاشة الاستلام لا تحتوي على زر "إضافة استلام علف"

## الحل المنفذ

### 1. تطبيق سطح المكتب (`feed_screen.dart`)

#### التغييرات:
✅ **إضافة زر ديناميكي حسب التبويب**:
- في تبويب "الاستهلاك": يظهر زر "إضافة استهلاك"
- في تبويب "الاستلام": يظهر زر "إضافة استلام"

✅ **إضافة دالة `_showAddReceivedDialog()`**:
- تفتح نافذة حوار لإدخال بيانات استلام العلف
- تتضمن الحقول:
  - التاريخ
  - الكمية (أكياس/كيلو/طن) مع تحويل تلقائي
  - نوع العلف (بادئ، نامي، بيّاض، رئيسي)
  - المورد (اختياري)
  - رقم الفاتورة (اختياري)
  - ملاحظات (اختياري)

✅ **إضافة كلاس `_FeedReceivedDialog`**:
- نافذة حوار كاملة لإدخال استلام العلف
- معاينة حية للكمية بالكيلوغرام
- التحقق من صحة البيانات قبل الحفظ
- ربط تلقائي بـ `farm_id` من بيانات المستخدم الحالي

#### الكود المضاف:
```dart
// سطر 95-130: دالة _showAddReceivedDialog
Future<void> _showAddReceivedDialog() async {
  final result = await showDialog<Map<String, dynamic>>(
    context: context,
    builder: (ctx) => const _FeedReceivedDialog(),
  );
  if (result != null && mounted) {
    final farmId = _farmId;
    // ... إنشاء السجل وحفظه
  }
}

// سطر 681-872: كلاس _FeedReceivedDialog
class _FeedReceivedDialog extends StatefulWidget {
  // ... حقول الإدخال
  // ... منطق التحويل بين الوحدات
  // ... واجهة المستخدم
}
```

### 2. تطبيق الجوال (Mobile)

#### الملاحظة:
تطبيق الجوال يحتوي بالفعل على شاشات منفصلة:
- `feed_consumption_screen.dart`: لاستهلاك العلف
- `feed_received_screen.dart`: لاستلام العلف

كل شاشة تحتوي على:
- ✅ تحديد تلقائي للمزرعة من بيانات المستخدم (`authProvider`)
- ✅ زر حفظ مدمج في الواجهة
- ✅ لوحة أرقام رقمية للإدخال السريع

#### التحسين المقترح (للتطبيق المستقبلي):
يمكن إضافة زر "إضافة سريع" في الشاشة الرئيسية للجوال يوجه مباشرة لهذه الشاشات.

## هيكل البيانات المستخدم

### نموذج استلام العلف (`FeedReceivedModel`):
```dart
{
  id: String?,
  farmId: String,        // من authProvider
  date: DateTime,
  entryMode: FeedEntryMode,  // bags/kg/ton
  quantity: double,          // الكمية المدخلة
  quantityKg: double,        // الكمية المحولة لكغ
  feedType: FeedType,        // starter/grower/layer/main
  supplier: String?,
  invoiceNumber: String?,
  notes: String?,
  syncStatus: SyncStatus.pending,
  createdAt: DateTime.now(),
}
```

### الثوابت المستخدمة:
```dart
AppConstants.kgPerBag = 50    // كل كيس 50 كغ
AppConstants.kgPerTon = 1000  // كل طن 1000 كغ
```

## خطوات الاختبار

### على تطبيق سطح المكتب:
1. افتح تطبيق المدير (Desktop)
2. انتقل إلى قسم "العلف"
3. جرب التبويبين:
   - **تبويب الاستهلاك**: اضغط "إضافة استهلاك" → أدخل البيانات → احفظ
   - **تبويب الاستلام**: اضغط "إضافة استلام" → أدخل البيانات → احفظ
4. تحقق من ظهور السجلات في الجداول
5. تأكد من حساب الكمية بالكيلوغرام بشكل صحيح

### على قاعدة البيانات:
```sql
-- التحقق من السجلات الجديدة
SELECT * FROM feed_received ORDER BY created_at DESC LIMIT 5;
SELECT * FROM feed_consumption ORDER BY created_at DESC LIMIT 5;

-- التحقق من حالة المزامنة
SELECT table_name, status, COUNT(*) 
FROM sync_changes 
WHERE table_name IN ('feed_received', 'feed_consumption')
GROUP BY table_name, status;
```

## الملفات المعدلة

| الملف | التغييرات | الأسطر |
|-------|-----------|--------|
| `/workspace/apps/desktop/lib/features/feed/presentation/feed_screen.dart` | إضافة زر ديناميكي + دالة `_showAddReceivedDialog` + كلاس `_FeedReceivedDialog` | 176-187, 95-130, 681-872 |

## المميزات المضافة

1. ✅ **زر إضافة مخصص لكل تبويب**
2. ✅ **نافذة حوار كاملة لاستلام العلف**
3. ✅ **تحويل تلقائي بين الوحدات (أكياس/كيلو/طن)**
4. ✅ **معاينة حية للكمية بالكيلوغرام**
5. ✅ **التحقق من صحة البيانات**
6. ✅ **ربط تلقائي بالمزرعة**
7. ✅ **حقول اختيارية للمورد والفاتورة والملاحظات**
8. ✅ **تكامل مع نظام المزامنة (SyncStatus.pending)**

## الخطوات التالية الموصى بها

1. **الاختبار**: تشغيل التطبيق واختبار الوظائف الجديدة
2. **المراجعة**: التحقق من أن البيانات تُحفظ بشكل صحيح في SQLite
3. **المزامنة**: التأكد من أن السجلات تُزامن مع Supabase بنجاح
4. **التوثيق**: تحديث دليل المستخدم بشرشحة كيفية استخدام الوحدة

## ملاحظات للمبرمجين

- **لا حاجة لتعديل قاعدة البيانات**: الحقول موجودة مسبقاً
- **لا حاجة لتعديل API**: يستخدم Repository المحلي الموجود
- **التوافق**: الكود متوافق مع إصدارات Flutter 3.x
- **اللغة**: الواجهة عربية بالكامل مع دعم RTL

---

**الحالة**: ✅ مكتمل  
**التاريخ**: 2025-01-02  
**المطور**: فريق التطوير
