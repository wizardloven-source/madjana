-- ============================================================
-- 006: تسعير العلف المستلم (المدير يسجّل سعر الكيلوغرام)
-- عند التسعير تُنشأ فاتورة مصروف تلقائياً في تطبيق سطح المكتب
-- آمن لإعادة التنفيذ
-- ============================================================

ALTER TABLE feed_received ADD COLUMN IF NOT EXISTS price_per_kg double precision;

NOTIFY pgrst, 'reload schema';
