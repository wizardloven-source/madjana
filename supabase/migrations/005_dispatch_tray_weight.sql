-- ============================================================
-- 005: وزن الصحن في التخريج (وزن 30 بيضة بالكيلوغرام)
-- اختياري — يقيس متوسط حجم البيض المخّرج
-- آمن لإعادة التنفيذ
-- ============================================================

ALTER TABLE egg_dispatch ADD COLUMN IF NOT EXISTS tray_weight_kg double precision;

NOTIFY pgrst, 'reload schema';
